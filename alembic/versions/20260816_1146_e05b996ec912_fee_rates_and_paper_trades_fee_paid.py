"""fee rates and paper trades fee_paid

Revision ID: e05b996ec912
Revises: a861ae5f413c
Create Date: 2026-08-16 11:46:23.510127

"""

from collections.abc import Sequence

import sqlalchemy as sa

from alembic import op

# revision identifiers, used by Alembic.
revision: str = "e05b996ec912"
down_revision: str | None = "a861ae5f413c"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # NOTE: autogenerate also proposed creating ix_prices_asset_captured_at
    # here - pre-existing drift unrelated to this migration (PriceSnapshot
    # already declares this index in app/db/models/markets.py; the DB copy
    # is missing it for reasons predating this change), stripped out again
    # same as the last two migrations that hit it. Needs its own dedicated
    # migration, tracked separately.
    op.create_table(
        "fee_rates",
        sa.Column("condition_id", sa.String(length=66), nullable=False),
        sa.Column("asset", sa.String(length=80), nullable=False),
        sa.Column("rate", sa.Numeric(precision=24, scale=6), nullable=False),
        sa.Column(
            "captured_at",
            sa.DateTime(timezone=True),
            server_default=sa.text("now()"),
            nullable=False,
        ),
        sa.PrimaryKeyConstraint("condition_id", "asset", name=op.f("pk_fee_rates")),
    )
    op.add_column(
        "paper_trades", sa.Column("fee_paid", sa.Numeric(precision=24, scale=6), nullable=True)
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("paper_trades", "fee_paid")
    op.drop_table("fee_rates")
    # ### end Alembic commands ###
