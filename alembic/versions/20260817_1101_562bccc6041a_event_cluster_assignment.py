"""event cluster assignment

Revision ID: 562bccc6041a
Revises: e05b996ec912
Create Date: 2026-08-17 11:01:33.396926

"""

from collections.abc import Sequence

import sqlalchemy as sa

from alembic import op

# revision identifiers, used by Alembic.
revision: str = "562bccc6041a"
down_revision: str | None = "e05b996ec912"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # NOTE: autogenerate also proposed creating ix_prices_asset_captured_at
    # here - pre-existing drift unrelated to this migration (PriceSnapshot
    # already declares this index in app/db/models/markets.py; the DB copy
    # is missing it for reasons predating this change), stripped out again
    # same as the last several migrations that hit it. Needs its own
    # dedicated migration, tracked separately.
    op.add_column("markets", sa.Column("event_cluster_id", sa.String(length=320), nullable=True))
    op.create_index(
        op.f("ix_markets_event_cluster_id"), "markets", ["event_cluster_id"], unique=False
    )
    op.add_column(
        "scout_validation_windows",
        sa.Column("effective_forward_trade_count", sa.Integer(), nullable=True),
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("scout_validation_windows", "effective_forward_trade_count")
    op.drop_index(op.f("ix_markets_event_cluster_id"), table_name="markets")
    op.drop_column("markets", "event_cluster_id")
    # ### end Alembic commands ###
